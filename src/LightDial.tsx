import { Light } from "ts-3d-engine";

interface LightSettingsProps {
    lightSource: Light; // Assuming LightSource is the class you want to pass
}

const LightDial: React.FC<LightSettingsProps> = ({ lightSource }) => {
    return <>
    </>
}

export default LightDial;